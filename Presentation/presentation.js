//Start file when the HTML is ready
$(document).ready(function() { //Open backets for ready function

$(".rave-fields").click( function() {
	$(this).css("width", "50%")
	
})

}); //Close brackets for ready() function